# замени на свои точные значения из SonarCloud:
# Organization key: см. Organization → Administration → Organization key
sonar.organization=Andrew

# Project Key: в Project Information на странице проекта в SonarCloud
sonar.projectKey=cAndrew_crypto-signal-desk

sonar.sources=src
sonar.exclusions=**/node_modules/**,**/dist/**,**/*.config.*
sonar.sourceEncoding=UTF-8
# если появится coverage (Jest/Vitest) — добавишь:
# sonar.javascript.lcov.reportPaths=coverage/lcov.info
